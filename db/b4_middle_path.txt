> Task: Make both grid patterns identical.  First grid can be written by
> outputting true or false.  Second grid shows the solution.
// We have the following pattern:
//
// oxxoooo
//
// Print "oxx", then use a loop to print "oooo".
output = false;
output = true;
output = true;
loop:                    // Start of "loop".
    output = false;      // Print "o".
    int++;               // How many "o" have been printed.
    check int < 4;       // Have we printed < 4 "o"?
    jump if true: loop;  // If true, go to start of "loop".
int = 0;                 // Otherwise, reset number of "o".
